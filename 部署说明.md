# 猫咪时间报 · 云端同步部署说明

## 已完成

- ✅ 页面已接入 Supabase SDK
- ✅ 存储逻辑改造：本地 localStorage + 云端 Supabase 双写
- ✅ 实时订阅：一端修改，另一端自动同步
- ✅ 离线兜底：断网时存在本地，联网自动同步
- ✅ 同步状态角标：页面右上角显示同步状态
- ✅ 所有 UI 和功能保持不变

## 你需要做的 3 步

### 第 1 步：执行建表 SQL

1. 打开 [Supabase Dashboard](https://supabase.com/dashboard)，进入你的项目
2. 左侧菜单 → **SQL Editor** → 点 **New query**
3. 打开 `supabase-setup.sql` 文件，复制全部内容，粘贴进去
4. 点 **Run**（右下角绿色按钮）执行
5. 看到 "Success. No rows returned" 就成功了

### 第 2 步：开启 Realtime（实时订阅）

1. 左侧菜单 → **Database** → **Replication**
2. 找到 **supabase_realtime** 这一行，右边显示 "0 tables"，点它
3. 勾选 **workbench_data** 这张表
4. 点 **Save**

### 第 3 步：打开页面验证

打开 `workbench-mobile.html`，看右上角的同步状态角标：

| 状态 | 含义 |
|------|------|
| 🟢 已同步 | 云端连接正常，数据已同步 |
| 🔵 同步中... | 正在上传/下载数据 |
| 🟡 离线模式 | 没联网，数据只存在本地 |
| 🔴 同步失败 | 云端连接有问题，检查网络 |

## 多端同步验证方法

1. **电脑浏览器**打开工作台，做一个操作（比如新增一条记账）
2. 看右上角角标：先显示"同步中..."，然后变"已同步"
3. **手机 TRAE App** 打开同一个工作台页面
4. 右上角应该会弹出"数据已从云端同步"的提示，数据自动更新
5. 反过来在手机上操作，电脑上也会自动同步

> 注意：两端需要同时打开页面才能实时推送。如果手机端后台杀掉了，重新打开时会自动拉取最新数据。

## 技术细节

- **数据表**：`workbench_data`，以 `storage_key` 为唯一键，整份数据存在 `data` 字段（jsonb 类型）
- **同步策略**：本地优先，云端异步防抖（800ms）写入，避免频繁请求
- **实时推送**：Supabase Realtime（PostgreSQL logical replication）
- **设备区分**：每个浏览器/设备生成一个随机 `device_id`，避免自己触发自己的同步
- **离线兜底**：Supabase 不可用时自动降级为纯本地模式，不影响使用
- **数据安全**：当前 RLS 策略允许匿名读写（个人使用场景）。如果后续要多人使用，建议加认证

## 文件清单

- `workbench-mobile.html` — 主页面（已接入 Supabase）
- `supabase-setup.sql` — 建表 SQL 脚本
- `assets/` — 图片资源
